- name: Standard Linux Playbooks
  hosts: all
  become: yes
  tasks:
  - name: Install apt modules
    yum:
      name:
      - git
      - nginx
      - postgresql
      - postgresql-server
      state: present
  - name: Create temporary directory
    file:
      path: /tmp/joe
      state: directory